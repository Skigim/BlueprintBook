# BlueprintBook

Generic shapez-modding rules (UI/HUD conventions, workflow-skill requirements, verification discipline, tool-call efficiency) live in the parent `../CLAUDE.md` and are auto-loaded alongside this file — no need to repeat them here.

- Before touching modals or dialogs, read `../docs/shapez_dialog_api.md`. Before other engine integration (HUD extension, storage, migrations), read `../docs/shapez_engine_notes.md` for generic gotchas and `docs/shapez_engine_notes.md` (this project) for BlueprintBook-specific internals (storage/migration file layout, dialog state keys).
